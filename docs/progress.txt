# AquaCard ralph progress — улучшения профиля

Проект (ПЕРЕЕХАЛ): корень /Users/user/aquacard; исходники app/src/main/java/net/aquadx/aquacard/.
Сборка: cd /Users/user/aquacard && JAVA_HOME=/opt/homebrew/opt/openjdk@17 ANDROID_HOME=$HOME/Library/Android/sdk ./gradlew :app:testDebugUnitTest :app:assembleDebug --no-daemon --console=plain
План: docs/profile-enhancements-plan.md. PRD: docs/prd.json.

## Сделано (US-A..US-F, все passes:true)
- US-A maimai-lock: ProfileScreen без GameChips/game/loadedGame, титулы-константы; AquaUser.profilePicture; @Serializable домена.
- US-B обложки/аватар: Coil 2.7.0; data/AquaAssets.kt (jacketUrl=staticBase/d/mai2/music/{(id%10000).pad6}.png, avatarUrlOrNull=staticBase/uploads/net/portrait/{pfp}); res/drawable/ic_jacket_placeholder.xml; ui/.../AsyncArt.kt (JacketImage AsyncImage+painter; PlayerAvatar SubcomposeAsyncImage→InitialsAvatar; InitialsAvatar); обложки в Best/Score/Recent; аватар в шапке; стабильные key в items; AquaAssetsTest(5).
- US-C иконка: docs/icon.jpg(1024²) → sips → mipmap-*dpi/ic_launcher_image.png (108/162/216/324/432); adaptive XML background=image, foreground=transparent. В APK 5 PNG.
- US-D кэш: SettingStore.last_username; data/CachePolicy.kt (sealed Decision + decide, чистая, инъекция времени); data/ProfileCache.kt (CachedProfile @Serializable; filesDir/profile_v2_<user>.json; синглтон mem; толерантное чтение); ProfileScreen stale-while-revalidate (LaunchedEffect авто-загрузка, refresh-иконка вместо кнопки, username-guard, приоритет last_username); CachePolicyTest(5)+ProfileCacheTest(3).
- US-E изолированная чистка: ProfileScore/RecentEntry → плоские data class (sealed убран); AquaProfileService game/mai2/ хардкод; AquaProfileRepository без game; ScoreFormat без game-параметров; удалён dead-code (AquaService/GameSummaryResponse/LinkCardResponse/createService/linkCard); ApiParseTest→GameSummary; chu-тесты убраны; HostDerivationTest под allMusicUrl(baseUrl); ProfileCacheTest под плоский ProfileScore.
- US-F верификация.

## Контекст/грабли
- Шелл с errexit — для grep использовать `set +e` или `if grep; then; else; fi`.
- Числовой формат — Locale.US (иначе ru-локаль ломает).
- Гонка фоновой сборки: новые .kt подхватываются текущей сборкой — не запускать сборку одновременно с записью зависимых файлов.

## Верификация (US-F)
- ./gradlew testDebugUnitTest assembleDebug => BUILD SUCCESSFUL. 41 тест, 0 failures/errors.
  ApiParseTest 1, AquaAssetsTest 5, CachePolicyTest 5, CardFormatTest 5, CryptoTest 2, HostDerivationTest 2, ProfileCacheTest 3, ProfileParseTest 6, ProfileRepositoryTest 3, ScoreFormatTest 9.
- grep: game== в profile UI = 0; sealed ProfileScore = 0; dead-code = 0; сервис mai2 = 5 эндпоинтов.
- APK 20.4 МБ → /Users/user/aquacard/AquaCard-debug.apk; иконка (5 PNG) в APK.

## Дальше (ralph): architect-верификация → deslop → регрессия → cancel.
